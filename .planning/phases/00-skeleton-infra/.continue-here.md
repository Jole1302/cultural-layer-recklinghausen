---
context: phase
phase: 00-skeleton-infra
task: 29
total_tasks: 35
status: paused_human_action
last_updated: 2026-04-29T14:20:00.000Z
---

# Resume Phase 0 — Skeleton & Infra

**Position:** 30/35 tasks done (86%). Wave 0 ✅, Wave 1a ✅, Wave 1b ✅, Wave 2 code-only ✅. Remaining: 5 human-action gates.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| vanilla-cookieconsent v3 ESM-CJS interop | Library publishes CJS; ESM `import()` exposes API on `.default`, not top-level. TypeScript types claim top-level. | advisory | Use `((mod as unknown) as { default: typeof mod }).default ?? mod` cast pattern (see src/components/cookie-banner.tsx) |
| `'server-only'` import breaks vitest | `'server-only'` package throws when imported outside React Server Components context; vitest runs in plain Node. | advisory | vitest.config.ts has `resolve.alias['server-only'] = './tests/setup/server-only-stub.ts'` |
| Vitest 4 poolOptions removed from `test` | Vitest 4 moved poolOptions out of `test:`; type defs don't accept it top-level either. | advisory | Removed entirely; default `singleFork: false` is what we want |
| `vercel env add NAME preview` requires git | Vercel CLI needs a connected Git repo before per-branch preview env vars can be added. | advisory | Defer preview env vars until T-30 (GitHub repo + Vercel Git connection) |
| Drizzle wraps pg errors | `await db.insert(...).rejects.toThrow(/regex/)` matches the wrapped Drizzle error message, not the original pg error. | advisory | Use `try/catch` and inspect `err.cause.code` (e.g. `'23505'` for unique violation) and `err.cause.constraint` (see tests/unit/audit.test.ts) |
| `vercel blob create-store` interactive prompts | The CLI prompts for env-multi-select via arrow keys; piped stdin doesn't work. | advisory | Use `--yes` flag — it auto-selects all envs when paired with `--access` |
| `.env.local` double-quotes (Vercel CLI format) | `vercel env pull` emits `KEY="value"` with double-quotes; plain `string.trim()` does NOT strip them. | advisory | tests/setup/load-env.ts strips surrounding double-quotes; production Next.js parses them correctly via dotenv |
| Playwright e2e in vitest runner | Without exclude, vitest picks up tests/e2e/*.spec.ts and crashes with "Playwright Test did not expect test() to be called here" | advisory | vitest.config.ts has `exclude: ['**/node_modules/**', '**/tests/e2e/**']` |
| `@sentry/nextjs` v10 removed `hideSourceMaps` | SentryBuildOptions v10.50.0 no longer has `hideSourceMaps`; use `sourcemaps` object instead. | advisory | Removed from next.config.ts; widenClientFileUpload kept (still valid) |
| Better Auth tables need manual pg migration | `drizzle-kit migrate` reads config `schema:` which points only to domain schema; Better Auth SQL must be applied via `pg.Pool.query()` directly or via a separate drizzle.config.ts. | advisory | Applied manually via node + pg Pool in T-25; journal updated to include 0001_better_auth entry |

## Required Reading (in order)

1. `.planning/PROJECT.md` — core value, locked decisions (DEC-001..DEC-022), constraints
2. `.planning/REQUIREMENTS.md` — REQ-quality-bar + REQ-audit-log acceptance criteria
3. `.planning/phases/00-skeleton-infra/00-PLAN.md` — task definitions T-29..T-34
4. `.planning/HANDOFF.json` — machine-readable state of all 35 tasks

<current_state>
Cloud foundation + all code complete. pnpm typecheck/lint/vitest run all GREEN.

Live infrastructure:
- Vercel project `cultural-layer-recklinghausen` linked (prj_9qOjJGmaWtzzJM5YWE5Vgbe7CtZ5)
- Neon Postgres `neon-claret-brush` provisioned; 14 tables live (10 domain + 4 Better Auth)
- Vercel Blob `cultural-layer-blob` (public, iad1) linked
- BETTER_AUTH_SECRET + BETTER_AUTH_URL set for production+development

Code shipped (Wave 2 code-only slice):
- T-25: Better Auth fully wired; smoke test 2/2 against Neon
- T-26: /api/_test-sentry route (double-gated, env+token)
- T-27: @sentry/nextjs installed; 4 config files + instrumentation.ts + next.config.ts wrapped
         [verify-step DEFERRED — needs SENTRY_DSN from user]
- T-28: vercel.json — Node 24 + 300s timeout (DEC-021)
- T-33: docs/runbook.md written

Phase 0 success criteria status:
- [x] SC#2: 10-table schema migrated to Neon with CHECKs + 8 indexes (T-17 LIVE)
- [x] SC#4: audit() helper unit-tested on Postgres testcontainers (T-20 PASSED)
- [ ] SC#1: CI gate + manual prod promote — requires GH connection (T-30) + Vercel toggle (T-29)
- [ ] SC#3: Sentry P1 alert to Telegram — requires Sentry account+keys+T-27 verify
</current_state>

<completed_work>

**Wave 0 (T-00..T-12) — Bootstrap, all auto:**
- Toolchain: Node 24.15 via nvm + symlinks in ~/.local/bin (~/.bashrc patched), pnpm 10.32, Vercel CLI 52.0.0
- Next.js 16.2.4 + React 19.2.5 scaffold (locked to RESEARCH.md)
- Strict TS (noUncheckedIndexedAccess, verbatimModuleSyntax, etc.)
- Husky + lint-staged + gitleaks pre-commit
- Vitest 4 + Playwright 1.59 + testcontainers 11.14 + axe
- vitest.config.ts, playwright.config.ts, tests/setup/pg-container.ts
- 4 GitHub Actions workflows (ci, e2e, lighthouse) + lighthouserc.json (mobile, 0.9 thresholds)
- drizzle.config.ts + drizzle-orm 0.45.2 + drizzle-kit 0.31.10 + @neondatabase/serverless 1.1.0

**Wave 1b (T-13..T-22) — Schema + Audit + Landing, 9 of 10 done:**
- src/lib/env.ts (zod-validated env reader)
- src/db/schema.ts (10 tables, 6 enums, 2 CHECKs via sql template, 4 UNIQUEs, 8 indexes)
- src/db/index.ts (Neon HTTP `db` + WS Proxy `dbTx`)
- drizzle/0000_productive_leader.sql (initial migration generated)
- src/lib/audit.ts (DI helper — accepts test db arg)
- src/lib/ratelimit.ts (Upstash sliding-window with NOOP fallback)
- tests/unit/audit.test.ts (5/5 testcontainers tests — REQ-audit-log SC#4 PROVEN)
- src/components/cookie-banner.tsx (vanilla-cookieconsent 3.1, DE)
- src/app/{layout,page}.tsx (a11y-first DE landing)
- tests/e2e/landing.spec.ts (3/4 pass + 1 documented skip)

**Wave 1a (T-23, T-24) + T-17 — Cloud provisioning, ALL done via Vercel MCP:**
- vercel link → cultural-layer-recklinghausen
- vercel integration add neon → DATABASE_URL + 14 PG envs auto-injected
- vercel blob create-store --yes → BLOB_READ_WRITE_TOKEN
- BETTER_AUTH_SECRET (crypto) + BETTER_AUTH_URL pushed to prod+dev
- vercel env pull → .env.local (gitignored)
- pnpm db:migrate → 10 tables LIVE in Neon (verified)

**Wave 2 code-only (T-25..T-28, T-33) — 5 tasks, all done:**
- T-25: better-auth@1.6.9 + resend@6.12.2 installed; src/lib/auth.ts, src/lib/auth-client.ts,
  src/db/auth-schema.ts, src/app/api/auth/[...all]/route.ts, tests/integration/better-auth-smoke.test.ts
  (2/2 pass against Neon). databaseHooks mirrors new auth users to domain users table (T-0-07 mitigation).
  drizzle/0001_better_auth.sql applied live to Neon (4 tables: user, session, account, verification).
- T-26: src/app/api/_test-sentry/route.ts — double-gated (ENABLE_TEST_SENTRY + DEBUG_TOKEN)
- T-27: @sentry/nextjs@10.50.0 installed; sentry.{client,server,edge}.config.ts + instrumentation.ts
  + next.config.ts wrapped with withSentryConfig + cacheComponents=true. VERIFY STEP DEFERRED.
- T-28: vercel.json — nodejs24.x + maxDuration:300 for all API routes (DEC-021)
- T-33: docs/runbook.md — services, secret rotation, deployment, Sentry, migration, PII/GDPR, cold-start

**Rule 1 auto-fixes applied in Wave 2:**
- tests/setup/load-env.ts: strip double-quotes from Vercel CLI env format
- vitest.config.ts: exclude tests/e2e from vitest runner
- hideSourceMaps removed from next.config.ts SentryBuildOptions (not in v10.50.0 API)
</completed_work>

<remaining_work>

**Wave 2 blocked (T-29..T-32, T-34) — 5 tasks, ALL human-action gates:**

- T-29: Vercel Dashboard — "Stage and manually promote production deployments" toggle (DEC-022 + DEC-020).
  Browser-only; no CLI flag available. Generate Bypass Token, push VERCEL_AUTOMATION_BYPASS_SECRET.
- T-30: Create GitHub repo + push + connect Vercel GH integration in Vercel UI.
  `gh repo create cultural-layer-recklinghausen --private --source=. --push`
  Then: Vercel Dashboard → Project → Settings → Git → Connect to Repository
- T-31: GitHub branch protection rules — requires T-30 first. Protect `main`:
  require PR + status checks (typecheck, lint, vitest, secret-scan, playwright).
- T-32: First manual production promote + Sentry smoke verify (ENABLE_TEST_SENTRY=true on preview).
  Requires: T-29 toggle + T-30 GH connection + Sentry DSN in ENV.
- T-34: Final phase verification — Sentry P1 → Telegram, Lighthouse mobile ≥ 0.9, axe 0 violations.
  Requires: T-30 (preview URL) + Sentry fully wired.
</remaining_work>

<decisions_made>

- Drizzle (orm + kit + neon-serverless) installed in Wave 0 not Wave 1b — needed for T-09 typecheck
- Cookie banner test SKIPPED — v3 only auto-shows when >=1 category non-readOnly (Phase 0 ships only readOnly necessary by design); banner CODE is wired
- Cloud setup automated via Vercel MCP (`vercel:bootstrap` skill + Vercel CLI) — Vercel/Neon/Blob/auth-secret done programmatically; Sentry/Resend/Upstash deferred to user since no MCP plugins
- T-17 (drizzle migrate) executed early — was Wave 1b but unblocked the moment DATABASE_URL became live in Wave 1a
- BETTER_AUTH_URL preview deferred to post-GH-connection — Vercel CLI rejects per-branch env adds without connected git
- T-27 verify step skipped — Sentry DSN requires user account creation; code complete and typechecks green
- hideSourceMaps not valid in @sentry/nextjs v10.50.0 SentryBuildOptions — removed
- .env.local double-quote stripping added to load-env.ts (Rule 1 auto-fix)
- tests/e2e excluded from vitest runner (Rule 1 auto-fix — was crashing vitest with Playwright error)
- Better Auth tables applied via pg.Pool directly (drizzle-kit migrate only covers schema.ts; Better Auth SQL is separate)
</decisions_made>

<blockers>

- **Sentry account** — required for T-27 verify + Phase 0 SC#3. Code shipped; verification gate stays open until DSN reaches Vercel ENV.
- **Resend API key** — env.ts requires `re_*` prefix; placeholder in .env.local lets local code parse but real key needed for any actual email send.
- **Upstash Redis** — ratelimit.ts has NOOP fallback so non-blocking; still required for production target.
- **GitHub repo + Vercel Git connection** — preview deploys, e2e workflows, lighthouse workflows all triggered by `deployment_status` webhook from Vercel; without GH connection none of them run.
- **Vercel Deployment Protection toggle** — DEC-022 requires manual prod promotion; no CLI flag for this toggle, must be done in Vercel Dashboard UI.
</blockers>

## Infrastructure State

- **Vercel:** project linked (jole1302s-projects/cultural-layer-recklinghausen, ID prj_9qOjJGmaWtzzJM5YWE5Vgbe7CtZ5)
- **Neon DB:** `neon-claret-brush` live, 14 tables (10 domain + 4 Better Auth), host `ep-holy-frog-amziaatt-pooler.c-5.us-east-1.aws.neon.tech`
- **Vercel Blob:** `cultural-layer-blob` (store_WYrWSfO3lUpmdSND), public, iad1, linked to project
- **Vercel ENV (cloud):** 20 vars in production+preview+development (DATABASE_URL, DATABASE_URL_UNPOOLED, 14 PG/POSTGRES_*, BLOB_READ_WRITE_TOKEN, BETTER_AUTH_SECRET, BETTER_AUTH_URL, VERCEL_OIDC_TOKEN). Missing: SENTRY_DSN/AUTH_TOKEN/ORG/PROJECT, RESEND_API_KEY, UPSTASH_REDIS_REST_URL/TOKEN, VERCEL_AUTOMATION_BYPASS_SECRET, ENABLE_TEST_SENTRY, DEBUG_TOKEN, ADMIN_EMAIL.
- **.env.local:** 21 lines (20 Vercel-injected + RESEND_API_KEY placeholder), all real secrets gitignored (NEVER commit/echo)
- **GitHub:** gh CLI authenticated as Jole1302. No repo created yet for this project.
- **Local toolchain:** Node 24.15.0, pnpm 10.32.1, Vercel CLI 52.0.0

<context>
Phase 0 Wave 2 code-only slice completed in second session. All 5 code-only tasks done
(T-25 Better Auth wiring, T-26 Sentry test route, T-27 Sentry config files, T-28 vercel.json,
T-33 runbook). pnpm typecheck + lint + vitest run (7/7) all green.
Remaining 5 tasks are pure human-action gates that cannot be automated.
</context>

<next_action>
Human actions required in this order:
1. Register Sentry account → create project → add SENTRY_DSN/SENTRY_AUTH_TOKEN/SENTRY_ORG/SENTRY_PROJECT to Vercel ENV
2. `gh repo create cultural-layer-recklinghausen --private --source=. --push` then Vercel UI GH connect (T-30)
3. Enable "Stage and manually promote" in Vercel Dashboard (T-29)
4. After GH connect: GitHub branch protection rules (T-31)
5. First prod promote + Sentry smoke verify (T-32)
6. Final phase verification: Lighthouse + axe + Sentry→Telegram (T-34)
See docs/runbook.md for exact steps and CLI commands.
</next_action>
